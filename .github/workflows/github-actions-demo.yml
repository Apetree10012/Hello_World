name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
      # run: echo 
     #  "🎉 The job was automatically triggered by a ${{ github.event_name }} event." # "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!" #   echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."  checkout: repository code actions/checkout@v4
    #  echo "💡 The ${{ github.repository }} repository has been cloned to the runner."  # echo "🖥️ The workflow is now ready to test your code on the runner."  List files: in the repository  run:| -ls ${{ github.workspace }}
      # echo "🍏 This job's status is ${{ job.status }}."
